interface:
  display_name: "Create Plan PDF"
  short_description: "从需求澄清、方案研究到正式PDF交付的完整Codex工作流"
  default_prompt: "使用 $create-plan-skill 澄清需求、研究可行方案，并交付经过验证的正式PDF方案。"
policy:
  allow_implicit_invocation: false
