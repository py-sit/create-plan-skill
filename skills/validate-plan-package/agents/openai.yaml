interface:
  display_name: "Validate Plan Package"
  short_description: "独立验收方案源文件、图表、来源和 PDF"
  default_prompt: "使用 $validate-plan-package 只检查现有方案包并报告缺陷，不要擅自重写。"
policy:
  allow_implicit_invocation: false
